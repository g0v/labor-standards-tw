import {Duration, Labor} from './'

export default class WorkTime {
  constructor (duration: Duration, labor: Labor) {
    // TBD
  }
  validate () {
    // TBD
  }
  add (date: Date, hours: number, holiday?: boolean) {
    // TBD
  }
  overtimePay (accident?: boolean, aggreed?: boolean) {
    // TBD
  }
}
